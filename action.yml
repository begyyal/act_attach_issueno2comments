name: 'Revise git comments'
inputs:
  issueNo:
    required: true
  token:
    required: false
    default: ${{ github.token }}
  repos:  
    required: false
    default: ${{ github.repository }}
  prefix:
    required: false
    default: 'feature'
  from:
    required: false
    default: 'develop'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.token }}
    - ${{ inputs.repos }}
    - ${{ inputs.issueNo }}
    - ${{ inputs.prefix }}
    - ${{ inputs.from }}
